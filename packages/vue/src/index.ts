export { reactive, effect, ref, computed } from '../../reactivity/src'

export { watch, h, Text, Comment, Fragment, createElementVNode,createCommentVNode } from '../../runtime-core/src'

export { render,createApp } from '../../runtime-dom/src'

// export { compile } from '../../compiler-dom/src'
export { compile } from '@vue/vue-compat'
export { toDisplayString } from '@vue/shared'

